# ROVU

**Repository Overview Utility**

ROVU is a lightweight command-line tool written in Go for quickly inspecting a local source code repository.

## Features

Currently, ROVU supports basic repository scanning.

```bash
rovu scan .